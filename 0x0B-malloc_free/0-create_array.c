#include <stdlib.h>
#include "main.h"

/**
 * *create_array - It creates an array of chars,
 * and will initializes it with a specific char
 * @size: size of the array to create
 * @c: char to initialize the array c
 * Return: pointer to the array (Success), NULL (Error)
 */
